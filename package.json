{
  "name": "share2d",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["client", "server"],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "start:prod": "npm install --include=dev && npm run build && npm run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
